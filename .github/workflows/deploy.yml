# Deployment is handled by the gh-pages branch.
# See: https://halbertluong.github.io/ColoringApp/
name: No-op
on:
  push:
    branches: [main]
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Site is deployed via the gh-pages branch"
